class Button {
  String text;
  int width;
  int height;
  int radius;
  int fontSize;
  int padding;

  Button(this.text, {required this.width, required this.height, required this.radius, required this.fontSize, required this.padding});
}

// positional Parameter: text
// named Parameter: width, height, radius, fontSize, padding;